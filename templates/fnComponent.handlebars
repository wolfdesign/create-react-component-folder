import React from 'react';
import { _{{ name }} } from './styles';


export type I{{ name }} = {

};

export const {{ name }} = (props: I{{ name }}) => {
  const { } = props;

  return (
    <_{{ name }}>{{ name }}</_{{ name }}>
  );
};
