import React from 'react';
import { _{{ name }} } from './{{ name }}.styles';


export type I{{ name }} = {
    name?: string;
};

export const {{ name }} = (props: I{{ name }}) => {
  const { name } = props;

  return (
      <_{{ name }}>
          {name}
      </_{{ name }}>
  );
};
