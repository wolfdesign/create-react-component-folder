import React from 'react';
{{#if styled}}import { _{{ name }} } from './styles';{{/if}}


{{#if export}}export {{/if}}type I{{ name }} = {
  //
};

{{#if export }}export {{/if}}const {{ name }} = (props: I{{ name }}) => {
  const { // } = props;

  return (
    <_{{ name }}>{{ name }}</_{{ name }}>
  );
};
